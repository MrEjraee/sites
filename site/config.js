// After deploying your Cloudflare Worker, paste its URL here (no trailing slash).
// Example: "https://lesson-hub.yourname.workers.dev"
window.HUB = {
  WORKER_URL: "https://computing-eta.vercel.app"
};
